<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="tieudiemview"
            type="thoxinhdep.kbbk.activity.main.fragment.tieudiem.entity.TieuDiemView"/>
        <variable
            name="adapter"
            type="thoxinhdep.kbbk.activity.main.fragment.tieudiem.adapter.CustomTieuDiemAdapter"/>
    </data>
    <merge>
        <ImageView
            android:id="@+id/imageAvatar"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            bind:image="@{tieudiemview.avatarUrl}"
            bind:onClick="@{tieudiemview.txtUrlId}"/>
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:alpha="0.7"
            android:background="@color/grey"
            android:id="@+id/relativeLayout"
            android:layout_alignParentBottom="true"/>
        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:gravity="center"
            android:text="@{tieudiemview.txtTitle}"
            android:textColor="@color/yellow"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_alignParentBottom="true"/>
    </merge>
</layout>
